# etch-a-sketch
A browser based sketchpad built with vanilla JavaScript. Hover over the grid to draw, adjust grid size dynamically, and rest the canvas. This is using DOM manipulation and event handling.
